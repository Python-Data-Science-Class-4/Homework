a = list(input("Bir metin giriniz:"))
b = int(input("Bir Sayı Giriniz:"))

c = 0

print(f"Girilen Metin: {a}")
while c < b:
    a.insert(0,a[len(a)-1])
    del a[len(a)-1]
    c = c + 1
    print(f"{c}. Tur: {a} ")
print("İşlem Bitti")

