metin = list(input("Bir Metin Giriniz:"))

sayilan_harf = []

for a in metin:
    b = metin.count(a)
    if (a not in sayilan_harf):
        print(f"Metin içerisinde {b} tane {a} harfi vardır.")
        sayilan_harf.append(a)
        